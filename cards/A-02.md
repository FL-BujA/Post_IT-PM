# PORTION A-02 — services: FlowService — project, cycle, gate, action

Goal: services/flow.py implementing the first six methods of C3.3 FlowService.
Five of them delegate to services already built; one is new.

Delegation map (the built method is in core.sig.txt — do not open its source):

  C3.3 create_project(name, sponsor, target_date, objective,
                      charter_text='', constraints_text='') -> ProjectRow
     -> ProjectSVC.create_project(name, target_date, sponsor='TBD',
            objective='', charter_text='', constraints_text='')
     Same parameters, DIFFERENT ORDER. Map them by name, not by position.

  C3.3 open_cycle(project_code, name) -> CycleRow
     -> PhaseSVC.open_cycle — identical signature, pass through.

  C3.3 close_cycle(project_code, gate_id) -> CycleRow
     -> PhaseSVC.close_cycle — identical signature, pass through.

  C3.3 add_action(project_code, title, owner, description='', priority=9,
                  due_start=None, due_end=None, cycle_id=None) -> ActionRow
     -> ActionsSVC.add_action — identical signature, pass through.

  C3.3 set_action_status(project_code, action_id, new) -> ActionRow
     -> ActionsSVC.change_status(project_code, action_id, new)
     Rename only.

  C3.3 record_gate(project_code, gate_id, outcome, actual_date=None) -> GateRow
     NEW. No built service covers it. Delegate to the data layer's
     GateRepo.record_outcome (signature in core.sig.txt).

Contract: 03-CONTRACTS-services.md (C3.3, first six methods)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   services/flow.py
  TESTS    tests/svc/test_flow.py
  REGISTER services/compose.py (LINE EDIT ONLY: flow slot -> FlowService)

Context Pack: this card · the C3.3 code block of contracts/03-CONTRACTS-services.md ·
  core.sig.txt for ProjectSVC, PhaseSVC, ActionsSVC and GateRepo signatures.
  The delegation map above is complete — do not open any .py file under
  services/ or data/.

Done when:
  - each of the six methods exists with the EXACT C3.3 signature, including
    parameter names, order and defaults. A test asserts this by
    inspect.signature against the contract, transcribed into the test.
  - create_project maps its parameters by NAME onto ProjectSVC's differing
    order. A test passes all six arguments positionally in C3.3 order and
    asserts the resulting row carries them correctly — this is the assertion
    that catches a positional mis-map.
  - open_cycle twice on one project raises ServiceError code 'cycle_open'
    (C3.3 frozen rule). If the built PhaseSVC does not enforce it, enforce it
    in FlowService rather than changing PhaseSVC.
  - close_cycle without a recorded outcome raises GateMissing, and a spy
    asserts the data call was not reached.
  - record_gate stores the outcome and returns a GateRow.
  - ServiceKit(root).flow is a real FlowService; the other five C3.1 slots
    still raise CoreError, and the eight legacy slots are untouched.

Forbidden: modifying services/projects.py, services/phase.py or
  services/actions.py — this card only delegates to them; duplicating their
  logic in flow.py; implementing add_minutes or list_for_project (A-03 owns
  them); editing anything under contracts/.

Stop and ask if: a signature this card names is absent from core.sig.txt, or a built method it delegates to does not exist. Report which one and stop — do not open source files to reconstruct it, and do not invent a replacement.

Commit: A-02: FlowService project, cycle, gate and action methods

Est. size: flow.py ~150, tests ~170. Total ~320.
