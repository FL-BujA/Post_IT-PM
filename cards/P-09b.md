# PORTION P-09b — data: report history + FTS5 search

Goal: data/reports_history.py and data/search.py — report provenance
rows plus the FTS5 search machinery over the tables indexed so far.

Contract: 02-CONTRACTS-data.md (C2.2 report history section, C2.2 search section)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  data/reports_history.py
  SOURCE  data/search.py
  TESTS   tests/data/test_reports_search.py

Context Pack: this card · 02-CONTRACTS-data.md (report history + search sections
  ONLY) · core.sig.txt · data/rows.py SIGNATURES · data/minutes.py
  SIGNATURES from P-09a (the search test writes minutes).

Done when:
  - report_history.add emits a REPORT event with ref reports/<id>.
  - report_history.list_for returns rows ordered generated_at DESC
    (fixture of 3 rows with distinct timestamps, exact id order asserted).
  - search returns the minutes row containing the unique token
    'quartz-cinder' when queried for it, with table 'minutes' in the
    result (the P-09a storage guarantee, now proven findable).
  - multi-word terms use FTS5 AND semantics: a two-token query returns
    ONLY the row containing both (fixture has one row per token and one
    row with both; assert exactly one hit).
  - project_code filter works (two-project fixture sharing a token,
    assert only the filtered project's row returns).
  - limit is honoured (fixture of 5 hits, limit=2, assert len == 2).
  - snippet is non-empty and contains the queried term (string assert).

Forbidden: touching data/__init__.py (the final slot fill is P-09c);
  implementing integrity checks here; re-sorting results in Python after
  FTS5 has ranked them (rank order is the contract).

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-09b: report history + FTS5 search

Est. size: reports_history ~70, search ~110, tests ~150. Total ~330.
