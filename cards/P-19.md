# PORTION P-19 — api: actions + evidence routes

Goal: api/routes_actions.py and api/routes_evidence.py — the loop and the
glue over HTTP. This is where the PM's daily work becomes short requests
(C4.0 acceptance: the whole loop from a text client).

Contract: 04-CONTRACTS-api.md (route table: actions, evidence, signals rows)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   api/routes_actions.py
  SOURCE   api/routes_evidence.py
  TESTS    tests/api/test_actions_evidence_routes.py
  REGISTER api/app.py (LINE EDITS ONLY: include the two routers)

Context Pack: this card · the actions/evidence/signals rows of the C4 route table
  plus C4.1 and C4.2 · C3.1 and C3.4 service SIGNATURES · conftest fixture.

Done when:
  test_loop_from_text_client — one test, nine requests, all green:
    POST /projects -> 201
    POST /projects/P001/actions "Buy the most expensive one" owner ana -> 201
    POST /projects/P001/priorities {"action_id":1} -> P1
    POST /projects/P001/evidence (form: file, purpose, ref_action) -> 201
    POST /projects/P001/actions/1/state {"to":"in_progress"} -> 200
    POST /projects/P001/actions/1/state {"to":"done"} -> 200
    POST /projects/P001/signals (kind=defer, owner=ana, note) -> 201
    GET  /projects/P001 -> 200, and the response carries the action with
    status done, the evidence with its purpose intact, and the signal in
    engagement_signals.
  - the same file attached twice returns 409 {code:'evidence_conflict'}.
  - DELETE evidence returns 204; the subsequent GET is 404, the row is
    gone, and the file is gone from the tmp workspace (assert on disk).
  - an illegal state transition over the API returns 409
    {code:'illegal_transition'}.

Forbidden: bucketing, sorting or deriving anything the service already
  returns; a route that bypasses the service layer to reach data.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-19: actions and evidence routes

Est. size: routes ~140 + ~120, tests ~220. Total ~480. The loop test carries most of it; if it grows past ~250 lines, emit SPLIT.
