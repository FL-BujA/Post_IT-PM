# PORTION P-25a — ui: meetings view

Goal: ui/views/meetings.html and meetings.js — minutes add and list,
minutes_text shown as plain text, and the link into the timeline through
the MEETING event that already exists.

Contract: C2.2 minutes (render contract)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   ui/views/meetings.html
  SOURCE   ui/views/meetings.js
  TESTS    tests/ui/test_meetings.py (Playwright)
  REGISTER ui/index.html (LINE EDIT ONLY: mount the view)

Context Pack: this card · C2.2 minutes row · the shell SIGNATURES from P-21b · the timeline jump target from P-22.

Done when:
  - the add form (title required, body optional, attendees as a comma
    list) POSTs and the new row appears in the list.
  - test_no_markdown_parser — typing '**bold**' in the body renders the
    literal asterisks in the DOM. This is CORRECT by contract and is the
    proof that no markdown parser is present.
  - the "in timeline" link on a meeting row navigates to the timeline
    view with that event's day header expanded.
  - an empty meetings list renders its frozen empty copy.

Forbidden: any markdown or rich-text rendering; creating MEETING events
  from the client (the service already emits them); a delete control.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-25a: meetings view

Est. size: meetings.html ~80, meetings.js ~70, tests ~90. Total ~240.
