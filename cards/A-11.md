# PORTION A-11 — data: backfill ProjectRepo.set_sponsor

Goal: C2.2 specifies ProjectRepo as having six methods:

    create, get, get_by_code, list, set_status, set_sponsor

set_sponsor was never built. C4.1 defines a route that needs it:

    POST /api/projects/{code}/set_sponsor
         body {"sponsor": str} -> {"project": ProjectView}

The contract comment calls sponsor "the 'prepared for' source" — it is what
a generated report defaults its Prepared for line to (C3.5).

This card adds set_sponsor ONLY. get_by_code is NOT in scope: the built
get(code) already takes a code, so the contract's get_by_code appears to be
the same method under a second name. That question is recorded in
BUILD_STATE.md rather than answered here.

Contract: 02-CONTRACTS-data.md (C2.2 ProjectRepo)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  data/projects.py
  TESTS   tests/data/test_projects_events.py (extend; do not rewrite)

Context Pack: this card · the C2.2 ProjectRepo block of
  contracts/02-CONTRACTS-data.md · data/projects.py, in Files allowed —
  set_status at line 140 is the pattern to mirror exactly: same validation
  shape, same return type, same error behaviour. Do not open any other
  .py file.

Done when:
  - ProjectRepo.set_sponsor(code, sponsor) -> ProjectRow, mirroring
    set_status: it updates the column, bumps updated_at the same way
    set_status does, and returns the fresh row.
  - an unknown code raises UnknownProjectData, as set_status does.
  - sponsor may be None: C3.3's create_project docstring says the field is
    open, and C3.5 renders a missing sponsor as an em dash. A test passes
    None and asserts the row stores NULL without raising.
  - an empty string is stored as given, not coerced to None — the two are
    different states and the report renders them differently.
  - test_set_sponsor_round_trip: set a sponsor, read the row back through
    get(), assert the value; then set a different one and assert it
    replaced rather than appended.
  - every existing test in tests/data/test_projects_events.py passes with
    its intent intact.
  - core.sig.txt regenerated (python3 tools/sigsheet.py) and `make gate`
    green.

Forbidden: adding get_by_code (out of scope, see the Goal); changing
  create, get, list or set_status; editing anything under contracts/;
  touching the gate table or data/gates.py (A-12 owns those).

Stop and ask if: ProjectRow has no sponsor field in core.sig.txt, or the project table has no sponsor column. Report which and stop.

Commit: A-11: backfill ProjectRepo.set_sponsor per C2.2

Est. size: projects.py +30, tests +50. Total ~80.
