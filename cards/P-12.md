# PORTION P-12 — services: evidence attach (the glue, end to end)

Goal: services/evidence.py — attach_evidence plus list/get/remove per
C3.4. This is the signature feature: attach -> store -> timeline.

Contract: 03-CONTRACTS-services.md (C3.4); C2.5 glue field list

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   services/evidence.py
  TESTS    tests/svc/test_evidence.py
  REGISTER services/__init__.py (LINE EDIT ONLY: evidence slot -> real)

Context Pack: this card · 03-CONTRACTS-services.md C3.4 · the C2.5 glue field list
  (transcribe it into the test as data) · core/paths.py SIGNATURES ·
  data EVIDENCE row SIGNATURES.

Done when:
  test_attach_flow — attach_evidence('evidence/P001/2026-07-14_bom.xlsx',
    kind 'spreadsheet', purpose 'BOM v3 baseline', ref to an action) on a
    pre-built project fixture:
      * evidence row with ALL glue fields stored (assert the 11 fields
        against the C2.5 list, transcribed into the test)
      * EVIDENCE event with a summary containing 'bom.xlsx'
      * list_for('P001') returns it first, created DESC, exactly 1 item
  test_ref_validation — ref_table='people' with a ref_id raises RefError
    (the allowed set is the 5 tables); ref_table='actions' with a ref_id
    that does not exist raises RefError. Both BEFORE any file work: assert
    the workspace is unchanged, no row, no event.
  test_path_safety — rel_path '../P001/x.pdf' raises PathEscape; '/x.pdf'
    raises PathEscape; 'evidence/P9/x.pdf' raises InvalidSlug. No row and
    no event in all three.
  test_remove — remove_evidence deletes the row AND the file and emits a
    REMOVED event (assert all three).
  test_determinism — the same attach twice from the same input file in two
    tmp dirs yields identical rel_paths and hashes (compare the evidence
    rows minus id and created_at).

Forbidden: deleting files anywhere except remove_evidence; normalising or
  rewriting rel_path (core validated it, this layer stores it verbatim);
  touching the timeline directly — events come from the data layer.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-12: evidence attach glue

Est. size: evidence ~150, tests ~180. Total ~330.
