# PORTION P-22 — ui: story timeline view (I1, rendered)

Goal: ui/views/timeline.html and timeline.js — the story view: vertical
timeline, day-grouped, event-coloured, cycle bands, gate markers. This is
the view the PM drives on a screen share.

Contract: C4.4 timeline GET shape · C1 EventRow fields

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   ui/views/timeline.html
  SOURCE   ui/views/timeline.js
  TESTS    tests/ui/test_timeline.py (Playwright)
  REGISTER ui/index.html (LINE EDIT ONLY: mount the view)

Context Pack: this card · C4.4 (transcribe the row shape into the Playwright
  fixture) · C1 EventRow fields · the P-21a/P-21b shell SIGNATURES.

Done when:
  - a fixture of 30 events across 6 days with 2 cycles and 2 gates
    renders: 6 day headers, 2 gate markers carrying the PASSED style
    class, and cycle bands whose DOM position precedes their first
    event's list item.
  - clicking an event opens the detail panel showing ref_table and
    ref_id, with an "open item" link for EVIDENCE, MINUTES and ACTION
    events. Clicking an EVIDENCE link navigates to the evidence view
    with that item selected — the cross-view jump.
  - the cycle filter: selecting cycle 2 renders only cycle-2 events
    (count assertion); "all" restores 30.
  - an empty project renders the exact frozen empty-state copy from C4.4.
  - test_no_client_sort — given an already-sorted fixture, the DOM order
    equals the input order exactly. The server sorts once; the view must
    not re-sort.

Forbidden: re-sorting the server's order; reading any event field not in
  the C1 contract; a second data source for the timeline.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-22: story timeline view

Est. size: timeline.html ~140, timeline.js ~200, tests ~180. Total ~520. If timeline.js exceeds 300 lines, emit SPLIT (render vs. filter).
