# PORTION P-15 — services: backup / restore / handover (I6)

Goal: services/backup.py — backup() (copy + sha256 + list), restore()
(I6: verify before replace), handover() (single-project zip). The
data-loss guarantee the whole tool rests on.

Contract: 03-CONTRACTS-services.md (C3.8 backup sections)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   services/backup.py
  TESTS    tests/svc/test_backup.py
  REGISTER services/__init__.py (LINE EDIT ONLY: backup slot -> real)

Context Pack: this card · 03-CONTRACTS-services.md backup sections ·
  C2.2 Integrity SIGNATURES · core hash SIGNATURES.

Done when:
  test_backup_completeness — project + 1 action + 1 attached real file ->
    backup() -> the BackupReport file exists in backups/, its list is
    non-empty, and the sha256 is stable (re-read, recompute, compare).
  test_restore_integrity — delete one evidence file, then restore(): the
    file is back with matching bytes by hash, the db row is present, and
    verify() reports ok=True (no half-state).
  test_restore_before_replace — corrupt the workspace DB, then restore():
    verify() ok=True afterwards, AND a spy on Integrity.verify asserts it
    was called BEFORE the file copy. Assert call order, not just outcome.
  test_handover — handover('P001') writes handover_out/<code>_handover_
    <ts>.zip; unzip into tmp and assert it contains the project's evidence
    directory and a single-project sqlite export whose action count equals
    the fixture count.

Forbidden: deleting the existing workspace before verify has passed;
  a partial restore path that can leave the workspace between states.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-15: backup, restore and handover (I6)

Est. size: backup ~220, tests ~220. Total ~440. At the ceiling — if backup.py exceeds 300 lines, emit SPLIT.
