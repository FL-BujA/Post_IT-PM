# PORTION P-21b — ui: shell behaviour (nav, switcher, theme persistence)

Goal: ui/app.js — view switching, the project switcher, theme toggle,
and the single permitted piece of client state (C4.7 folder memory).

Contract: 04-CONTRACTS-api.md (C4.7 state rule)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   ui/app.js
  TESTS    tests/ui/test_shell.py (Playwright, headless)
  REGISTER pyproject.toml (LINE EDIT ONLY: playwright pinned, dev extra)

Context Pack: this card · C4.7 · ui/index.html ids and ui/prism.css tokens from P-21a.

Done when:
  - clicking each of the six nav items switches the visible view
    container and leaves the console error list empty (assert on each).
  - the project switcher: with two projects from the stub API, select the
    second, reload the page, and the selection persists.
  - the localStorage key is EXACTLY 'pmckit.project' and it is the ONLY
    key ever written: a test enumerates localStorage after exercising
    every control and asserts the key set has exactly one member. The key
    name is frozen here so no later card introduces a second one.
  - the theme toggle persists across reload using that same single key's
    value object (no second key).

Forbidden: any localStorage key beyond the frozen one; caching API
  responses in client state; re-sorting or re-bucketing server data.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-21b: shell behaviour and folder memory

Est. size: app.js ~150, tests ~130. Total ~280.
