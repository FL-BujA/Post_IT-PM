# PORTION P-09 — data: Minutes + Signals + ReportHistory + Search + Integrity

Goal: data/minutes.py, data/signals.py, data/reports_history.py,
data/search.py, data/integrity.py — the remaining DataKit slots (C2.1
fully assembled after this card) and the FTS5 + I2 machinery.

Contract: 02-CONTRACTS-data.md (C2.2 remaining sections, C2.5 verify stub
shape is NOT this card — this is real verify)

Files allowed: data/minutes.py · data/signals.py · data/reports_history.py
               data/search.py · data/integrity.py
               tests/data/test_comms_reports.py
               tests/data/test_search_integrity.py
               data/__init__.py (LINE EDITS: final slot fill)

Context Pack: this card · 02-CONTRACTS-data.md (the 5 sections) ·
  core.sig.txt · data/rows.py SIGNATURES.

Done when:
  - minutes.add emits MEETING event; minutes_text stored verbatim and
    findable: a search test writes minutes containing the unique token
    'quartz-cinder' and search('quartz-cinder') returns the row
    (table 'minutes').
  - signals: insert emits SIGNAL event with summary containing 'Signal #<id>'
    (the frozen summary format, test asserts prefix 'Signal #');
    list_for filters (kind AND owner AND resolved each tested);
    set_resolved toggles + resolved_at.
  - report_history.add emits REPORT event (ref reports/id); list_for
    order generated_at DESC.
  - search: multi-word terms -> FTS5 AND semantics (test: two-token query
    returns only the row containing BOTH); project_code filter works;
    limit honored; snippet non-empty and contains the term (assert).
  - Integrity (I2 — named tests):
      test_i2_missing_file_flagged  — db row + deleted file -> ok=False,
                                      row in missing[], NOTHING deleted
                                      from the db (row count unchanged).
      test_i2_hash_mismatch         — file bytes changed after record ->
                                      mismatched[] non-empty.
      test_i2_orphan                — file on disk with no row -> orphans[]
                                      contains its rel_path.
      test_i2_clean                 — consistent workspace -> ok=True,
                                      all three lists empty.
  - DataKit now exposes ALL slots real (test asserts the full C2.1
    attribute set types) — DATA CONTRACT CLOSED.
  - `make gate` green → commit P-09 green.

Note: 5 source files for one card is an R10 stretch; they are small
(avg ~70 lines) and share one test story — the split is justified by the
shared FTS/integrity test narrative. If any file exceeds 300 lines,
SPLIT: and propose. (Logged: intentional cluster, R10 files-allowance
deviation recorded in BUILD_STATE.md per MOSAIC deviation logging.)

Est. size: minutes ~80, signals ~90, reports_history ~70, search ~110,
  integrity ~130, tests ~260.
