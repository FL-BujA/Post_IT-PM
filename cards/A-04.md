# PORTION A-04 — services: EngagementService

Goal: services/engagement.py implementing C3.4. Two methods delegate, two
are new.

  C3.4 record(project_code, kind, owner, action_id=None, note='') -> SignalRow
     -> ActionsSVC.add_signal — identical signature, pass through.

  C3.4 list_for(project_code, kind=None, owner=None, resolved=None)
       -> list[SignalRow]
     -> SignalRepo.list_for — identical, pass through.

  C3.4 mark_resolved(project_code, signal_id, resolved=True) -> SignalRow
     SignalRepo.set_resolved returns None; fetch and return the row.

  C3.4 health_by_owner(project_code) -> list[OwnerHealth]
     NEW. OwnerHealth is defined by this card as a frozen dataclass in
     services/engagement.py with the fields C3.4 names verbatim:
     owner: str, counts: dict[SignalKind, int], total: int, open_total: int.

Contract: 03-CONTRACTS-services.md (C3.4)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   services/engagement.py
  TESTS    tests/svc/test_engagement.py
  REGISTER services/compose.py (LINE EDIT ONLY: engagement slot -> real)

Context Pack: this card · the C3.4 code block of contracts/03-CONTRACTS-services.md ·
  core.sig.txt for ActionsSVC.add_signal, SignalRepo and SignalRow. Do not
  open any .py file under services/ or data/.

Done when:
  - all four methods exist with the EXACT C3.4 signatures, asserted by
    inspect.signature against the contract transcribed into the test.
  - OwnerHealth carries exactly the four C3.4 fields with those types.
  - health_by_owner aggregates ALL signals for the project by owner: a
    fixture of 9 signals across 3 owners and 4 kinds asserts the exact 3x4
    count matrix, plus total and open_total per owner.
  - health_by_owner is the ONLY aggregation of signal counts in the codebase
    (C3.4 frozen rule: the report strip and the engagement view both call it).
    A test greps services/ for a second aggregation and fails if one appears.
  - mark_resolved(resolved=False) reverses a resolution and clears
    resolved_at.
  - ServiceKit(root).engagement is real; the remaining C3.1 slots still
    raise CoreError.

Forbidden: duplicating the aggregation anywhere else; modifying
  services/actions.py; adding a fifth SignalKind (that is a CC card);
  deleting a signal — they are append-only.

Stop and ask if: a signature this card names is absent from core.sig.txt, or a built method it delegates to does not exist. Report which one and stop — do not open source files to reconstruct it, and do not invent a replacement.

Commit: A-04: EngagementService with OwnerHealth aggregation

Est. size: engagement.py ~130, tests ~170. Total ~300.
