# PORTION P-10a — services: ServiceKit composition + project lifecycle

Goal: services/compose.py (ServiceKit assembly with typed placeholder
slots) and services/projects.py (create_project full glue, update_project).
This card carries the 'one call, whole world' acceptance.

Contract: 03-CONTRACTS-services.md (C3.0 composition, C3.1 project section)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   services/compose.py
  SOURCE   services/projects.py
  TESTS    tests/svc/test_projects.py
  REGISTER services/__init__.py (LINE EDITS ONLY: compose re-export)

Context Pack: this card · 03-CONTRACTS-services.md C3.0 + the C3.1 project part
  ONLY (not the phase part) · core.sig.txt · C2.1 DataKit shape and
  data/rows.py SIGNATURES.

Done when:
  - test_one_call_whole_world — create_project("Alpha Bom", "2026-09-30",
    "TBD") on a FRESH empty tmp workspace produces, in ONE call:
      * project row with status 'planned'
      * charter event present (kind 'charter', summary contains 'Alpha Bom')
      * first open cycle row named 'Charter cycle'
      * evidence/, reports/ and backups/ directories created
      * root manifest.json exists with projects: ["P001"]
      * NO evidence rows (nothing attached yet)
    This is the P-10a signature test (00-ARCHITECTURE core acceptance,
    project side).
  - duplicate code raises ServiceError with code 'project_exists'.
  - update_project: name, target and prepared_for each persist and emit an
    UPDATE event naming the changed field in the summary (parametrized x3).
  - ServiceKit(tmp) exposes project_svc real, and typed placeholder slots
    for phase/actions/evidence/minutes/report/backup/integrity that raise
    CoreError until their card. One test asserts the full C3.0 attribute
    set exists from this card onward, so the shape never changes again.

Forbidden: implementing open_cycle or close_cycle (that is P-10b — the
  'Charter cycle' row here is created directly through the data layer, not
  through phase_svc); writing any HTML; reading evidence files.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-10a: ServiceKit composition and project lifecycle

Est. size: compose ~90, projects ~140, tests ~150. Total ~380.
