# PORTION P-24 — ui: evidence list, attach and search

Goal: ui/views/evidence.html and evidence.js — the glue view: attach,
list with purpose and notes, the "open item" panel, and the binding of the
shell's global search bar.

Contract: C2.3 Evidence row · C3.4 attach (the POST body keys are frozen: file, purpose, ref_action, ref_gate) · C2.2 search

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   ui/views/evidence.html
  SOURCE   ui/views/evidence.js
  TESTS    tests/ui/test_evidence_search.py (Playwright)
  REGISTER ui/index.html (LINE EDIT ONLY: mount the view)

Context Pack: this card · C2.3 + C3.4 form contract + C2.2 search · the shell
  search bar SIGNATURES from P-21b · the timeline jump target from P-22.
  The attach control is a plain <input type=file> styled by tokens; the
  drag area is div.dropzone. No new CSS file.

Done when:
  - attach via the file input with a fixture xlsx, a purpose string and
    ref_action: the POST body has EXACTLY the four frozen keys (assert
    set equality), and on success the row appears in the list with its
    purpose and a "view in timeline" link.
  - three-state rejection test: 409 shows the code in a toast and leaves
    the list unchanged and the input cleared; 400 likewise; 201 succeeds.
  - test_no_search_needed — from a board card's evidence link, the panel
    opens pre-selected and shows the placeholder box with the rel_path,
    the purpose, the notes, and the source event's timestamp.
  - test_search_jump — typing 'quartz-cinder' into the shell bar returns
    at least one hit, and clicking a hit navigates to that item's view.
  - search fires one GET per 250ms debounce, never on an interval
    (assert the request count for a burst of keystrokes).

Forbidden: a second file-listing source (evidence rows only); a custom
  file preview renderer (the placeholder box is the contract); polling.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-24: evidence view and search binding

Est. size: evidence.html ~180, evidence.js ~160, tests ~180. Total ~520. If evidence.js exceeds 300 lines, emit SPLIT (attach vs. search).
