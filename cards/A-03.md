# PORTION A-03 — services: FlowService — minutes and the project snapshot

Goal: Complete C3.3: add_minutes and list_for_project, plus the
ProjectSnapshot type the latter returns. Both are new; nothing built covers
them.

  C3.3 add_minutes(project_code, held_at, attendees, decisions,
                   agreed_actions, risks, minutes_text, cycle_id=None)
                   -> MinutesRow
     Delegate to MinutesRepo.add (signature in core.sig.txt). That repo
     returns int; fetch and return the MinutesRow.

  C3.3 list_for_project(project_code) -> ProjectSnapshot
     NEW aggregate. ProjectSnapshot is not defined anywhere yet — this card
     defines it as a frozen dataclass in services/flow.py.

Contract: 03-CONTRACTS-services.md (C3.3, add_minutes and list_for_project)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   services/flow.py
  TESTS    tests/svc/test_flow_snapshot.py
  REGISTER core.sig.txt (regenerate: python3 tools/sigsheet.py)

Context Pack: this card · the C3.3 code block of contracts/03-CONTRACTS-services.md,
  whose list_for_project docstring enumerates the snapshot contents verbatim ·
  core.sig.txt for the repo signatures and every row type. Do not open any .py
  file under data/.

Done when:
  - ProjectSnapshot is a frozen dataclass in services/flow.py with exactly
    the fields C3.3 names: project, current_cycle, actions, open_gates,
    events, minutes, signals. Every element type is a C2 row from
    core.sig.txt — no new row types.
  - list_for_project returns it with the C3.3 quantities and orders, each
    asserted separately against a fixture built to exceed every limit:
      actions of the current cycle, priority ascending
      open gates
      last 20 events, occurred_at descending
      last 5 minutes
      last 10 signals
    The fixture has 25 events, 8 minutes and 14 signals so the caps are
    proven rather than assumed.
  - list_for_project on an unknown code raises UnknownProjectService.
  - add_minutes returns a MinutesRow and the MEETING event is present.
  - it is ONE call: a spy asserts list_for_project issues no more data-layer
    calls than the snapshot requires, so the UI's one-query guarantee holds.
  - core.sig.txt regenerated and `make gate` green — ProjectSnapshot must be
    in the sheet before any api card can use it.

Forbidden: defining a new row type; re-sorting or filtering in a later
  layer what this method already orders; modifying the six methods A-02
  built.

Stop and ask if: a signature this card names is absent from core.sig.txt, or a built method it delegates to does not exist. Report which one and stop — do not open source files to reconstruct it, and do not invent a replacement.

Commit: A-03: FlowService add_minutes and list_for_project with ProjectSnapshot

Est. size: flow.py +130, tests ~180. Total ~310.
