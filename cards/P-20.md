# PORTION P-20 — api: reports/backup routes + API contract closure

Goal: api/routes_reports.py (meetings, report generation, backup,
restore, verify) — the last router. Then the API contract closes: the
entire route table green against BOTH the stub kit and a real kit.

Contract: 04-CONTRACTS-api.md (remaining route rows)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   api/routes_reports.py
  TESTS    tests/api/test_reports_backup_routes.py
  TESTS    tests/contracts/test_api_routes.py (the dual-kit suite;
           transcribe the route table into it as a data structure)
  REGISTER api/app.py (LINE EDIT ONLY: include the last router)

Context Pack: this card · the C4 route table in full · C3.5-C3.8 SIGNATURES.

Done when:
  - minutes POST and GET routes green against both kits.
  - POST /projects/{code}/report returns 200 with a ReportArtifact under
    each kit, and the test asserts the response KEYS are identical across
    kits — the seam's honesty is the point of the dual parametrization.
  - POST /backup returns 200 with a BackupReport; POST /restore returns
    200 and upholds I6 at the API level (both the good and the tampered
    case tested).
  - GET /integrity/verify returns 200 with a VerificationReport, tested
    clean and dirty.
  - the API suite has one test per route in the transcribed table, dual-kit
    parametrized, with envelope assertions on every 4xx through a shared
    check_envelope(r, code) helper in tests/contracts/helpers.py.

Forbidden: adding a route that is not in the transcribed table (a new
  route is a CC card); making a route pass by weakening check_envelope.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-20: reports routes and API contract closure

Est. size: routes ~130, suite ~250, tests ~80. Total ~460.
