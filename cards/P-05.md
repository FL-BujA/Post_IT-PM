# PORTION P-05 — data: schema, migrations, connection lifecycle

Goal: data/migrate.py + data/db.py per C2.0, C2.4, C2.5(DataKit init).
The database FILE is born here; every later repo card works against it.

Contract: 02-CONTRACTS-data.md (C2.0, C2.4)

Files allowed: data/migrate.py · data/db.py
               tests/data/test_migrate.py
               data/__init__.py (LINE EDITS: re-export DataKit, migrate)
               data/rows.py (SKELETON ONLY: all 10 row dataclasses with
                            fields per C2.0 + to_dict() — this is the one
                            exception where a shared artifact lands here
                            rather than its own card: without it, no repo
                            can be written)

Context Pack: this card · 02-CONTRACTS-data.md C2.0+C2.4+C2.5(DataKit
  init only) · core.sig.txt · core public errors (SIGNATURES).

Done when:
  - migrate(tmp) creates all 10 tables + fts_search + 5 indexes + unique
    evidence(rel_path); test enumerates via sqlite_master (parametrized
    list of expected names).
  - meta.schema_version == '1'; re-running migrate() on same file: no
    error, version still '1', tables untouched (idempotence test).
  - migrate raises on a file whose meta.schema_version is '2' (future
    protection — Code: CoreError 'unknown_schema') — test with a doctored
    file.
  - DataKit(path): connection in WAL mode (PRAGMA journal_mode == 'wal'),
    busy_timeout 5000, tx(identity fn) commits, tx(raising fn) rolls back
    — all three tested on a throwaway db.
  - row dataclasses: every field matches C2.0 column names exactly
    (a test compares dataclasses.fields(ProjRow) names to the list in
    the contract — transcribe C2.0 into the test as data, not comments).
  - `make gate` green — commit P-05 green.

Forbidden: any repository logic in db.py (lifecycle + tx only);
  importing services; a third-party dependency.

Est. size: migrate ~180 (schema string dominates), db ~60, rows ~220,
  tests ~150. rows.py is the big one — if it would exceed 300, split
  rows into rows_part1/2 and LOG the deviation in BUILD_STATE.md (R10
  split, not a violation)."
      }
    ],
    "is_error": false,
    "command": "cat /workspace/P-05.md",
    "exit_code": 0,
    "timeout": false,
    "metadata": {
      "exit_code": 0,
      "pid": -1,
      "username": "openhands",
      "hostname": "e8454e8f9da7",
      "working_dir": "/workspace/project/Post_IT-PM",
      "py_interpreter_path": "/usr/local/bin/python",
      "prefix": "",
      "suffix": "
[The command completed with exit code 0.]"
    },
    "full_output_save_dir": "/workspace/conversations/41e729eb92474f16ae7a9c05131f8733/observations",
    "kind": "TerminalObservation"
  },
  "action_id": "0af0cc9e-0e00-432c-8f76-8aa893d00942",
  "kind": "ObservationEvent"
}