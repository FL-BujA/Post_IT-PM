# PORTION P-01 — core: enums + error hierarchy

Goal: core/enums.py and core/errors.py exist exactly per contract C1.1
+ C1.1 addendum + C1.4. Every later layer imports from here.

Contract: 01-CONTRACTS-core.md (C1.1 incl. addendum, C1.4)

Files allowed: core/enums.py · core/errors.py
              tests/core/test_enums_errors.py  (their tests)
              core/__init__.py (LINE EDITS ONLY: add re-exports of the
              new public names — R7 targeted diff, no rewrite)

Context Pack: this card · 01-CONTRACTS-core.md · current core/__init__.py.

Done when:
 - all enums round-trip: value -> str -> Enum == value (parametrized test
   over ALL members of ALL 6 enums).
 - ALLOWED_ACTION_TRANSITIONS contains exactly the 5 frozen rules; a test
   imports it and checks each of the 5 source states' tuple.
 - every concrete error: isinstance(e, CoreError), e.code stable string,
   str(e) == message (12 errors + 2 data-level from C1.4; test constructs
   each with a code).
 - Illegal: cancelled -> done is NOT allowed (assert 'cancelled' maps to
   an empty tuple).
 - `make gate` green — commit P-01 green.

Forbidden: importing anything beyond stdlib (dataclasses, enum, typing);
putting a DB path or a string containing "D:\\" anywhere in core.

Est. size: enums ~90 lines, errors ~60 lines, tests ~80 lines.

"
      }
    ],
    "is_error": false,
    "command": "view",
    "path": "/workspace/P-01.md",
    "prev_exist": true,
    "old_content": null,
    "new_content": null,
    "kind": "FileEditorObservation"
  },
  "action_id": "57107876-74db-430b-b22d-9576f1004b05",
  "kind": "ObservationEvent"
}