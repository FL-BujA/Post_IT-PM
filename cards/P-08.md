# PORTION P-08 — data: ActionRepo + EvidenceRepo (invariant I4 lives here)

Goal: data/actions.py + data/evidence.py. I4: done->open increments
reopen_count and auto-emits the reopen signal. Evidence: the glue row
is recorded here (the file itself is services' job).

Contract: 02-CONTRACTS-data.md (C2.2 ActionRepo, EvidenceRepo)

Files allowed: data/actions.py · data/evidence.py
              tests/data/test_actions_evidence.py

Context Pack: this card · 02-CONTRACTS-data.md (those two sections) ·
 core.sig.txt (ALLOWED_ACTION_TRANSITIONS is in core — use it) ·
 data/rows.py + data/projects.py SIGNATURES.

Done when:
 - create emits ACTION_CREATED event (ref actions/id); defaults:
   priority 9, status open, reopen_count 0 (test asserts stored row).
 - state machine: EVERY frozen transition in ALLOWED_ACTION_TRANSITIONS
   accepted (parametrized over the dict — the test IS the contract),
   EVERY other pair rejected with code 'illegal_transition'
   (parametrized over the complement — 25 pairs; enumerate from the
   dict, not hard-coded, so the test stays true if a CC card changes
   the map).
 - I4: open->in_progress->done->open results in reopen_count == 1,
   last_reopened_at set, AND one engagement_signals row kind 'reopen'
   for the action — named test_i4_reopen_emits_signal.
 - started_at set on first in_progress, not on later re-entries;
   closed_at set exactly on the done/deferred/cancelled arrivals
   (assert NULL on open).
 - Evidence: record() persists ALL glue fields; duplicate rel_path ->
   EvidenceConflict (test both the error path and that the FIRST row
   survives untouched); get_by_path round-trip; list_for by project_code.
 - rel_path stored exactly as the input string (no normalization side
   effects — services validated it; data stores it verbatim, and a
   test passes a contract-valid path and asserts equality read-back).
 - `make gate` green — commit P-08 green.

Est. size: actions ~180, evidence ~90, tests ~220.

"
      }
    ],
    "is_error": false,
    "command": "view",
    "path": "/workspace/P-08.md",
    "prev_exist": true,
    "old_content": null,
    "new_content": null,
    "kind": "FileEditorObservation"
  },
  "action_id": "db087e9d-6e65-41dc-9c79-85d5dc52c471",
  "kind": "ObservationEvent"
}