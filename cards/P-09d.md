# PORTION P-09d — data: backfill SignalRow (and the two sibling row types)

Goal: Three row dataclasses named in the frozen C2.0/C2.2 contract were never
created: MinutesRow, SignalRow and ReportRow. The three repositories built in
P-09a and P-09b return list[tuple] where the contract specifies list[<X>Row].

This card adds all three row classes to data/rows.py and makes SignalRepo
honour its contract. MinutesRepo and ReportHistoryRepo are corrected in P-09e —
do not touch them here.

This is a backfill against a frozen contract, not a contract change. The
contract is correct; the implementation diverged. Nothing in contracts/ may be
edited by this card.

Contract: 02-CONTRACTS-data.md (C2.0 table definitions for minutes, engagement_signals and reports; C2.2 SignalRepo)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  data/rows.py
  SOURCE  data/signals.py
  TESTS   tests/data/test_comms.py (update the signals assertions)

Context Pack: this card · the C2.0 table definitions for the three tables and the
  C2.2 SignalRepo section of contracts/02-CONTRACTS-data.md · data/rows.py
  itself, which is in Files allowed and carries the pattern to follow ·
  core.sig.txt for anything else. Do not open any other .py file.

Done when:
  - data/rows.py gains MinutesRow, SignalRow and ReportRow, each following the
    existing pattern in that file exactly: a plain @dataclass, a docstring of
    the form """Table ``<name>`` (C2.0).""", field names that are the EXACT
    column names of the table in C2.0 and in the same order, and a to_dict()
    returning {column: value} in that same order.
  - the ten existing row classes are untouched (a test asserts ProjectRow's
    field tuple is unchanged, so this card cannot disturb them).
  - SignalRepo.list_for returns list[SignalRow], not list[tuple]. Every field
    is populated from the query; no field is left as None because it was not
    selected.
  - the existing signals tests are updated to read attributes rather than
    unpack tuples, and every assertion they made before still holds — the
    kind, owner and resolved filters, and the resolved_at stamp.
  - test_signalrow_matches_table — one test asserts SignalRow's field names
    equal the engagement_signals column names in declaration order, read from
    the schema rather than hard-coded. This is what makes the row and the
    table one truth rather than two.
  - SignalRepo.insert still returns int (the contract says int — do not
    change it).

Forbidden: editing anything under contracts/ — the contract is correct and
  frozen; touching data/minutes.py or data/reports_history.py (P-09e owns
  those); changing any repository's method names or parameters; adding fields
  to a row that are not columns of its table.

Stop and ask if: the C2.0 table definition for engagement_signals cannot be found after ONE read, or the column list in C2.0 disagrees with the table actually created by the migration. A disagreement between contract and schema is a defect this card cannot resolve — report it and stop.

Commit: P-09d: backfill SignalRow, MinutesRow, ReportRow; SignalRepo returns typed rows

Est. size: rows.py +90, signals.py ~15 changed, tests ~60 changed. Total ~165.
