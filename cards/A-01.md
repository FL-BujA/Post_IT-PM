# PORTION A-01 — services: add the six contract slots to ServiceKit

Goal: ServiceKit currently exposes eight slots named in C30_SLOTS
('project_svc', 'phase_svc', 'actions_svc', 'evidence_svc', 'minutes_svc',
'report_svc', 'backup_svc', 'integrity_svc'). C3.1 specifies six, differently
named: evidence, flow, engagement, report, backup, handover, plus a read-only
data property.

The eight existing slots were built against a section number that does not
exist in the contract. Rather than remove them, this card ADDS the six
contract slots alongside. The existing eight keep working and nothing already
committed is deleted; api/ will use only the six.

This card adds slots and placeholders only. The services behind them arrive in
A-02 through A-05.

Contract: 03-CONTRACTS-services.md (C3.1)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  services/compose.py
  TESTS   tests/svc/test_compose.py (extend; do not rewrite)

Context Pack: this card · the C3.1 code block of contracts/03-CONTRACTS-services.md,
  which lists the six slots verbatim · services/compose.py, in Files allowed —
  the _Placeholder class it already has is what the new slots use. Nothing
  else. This card needs no data-layer signatures.

Done when:
  - ServiceKit exposes evidence, flow, engagement, report, backup and
    handover, each a _Placeholder raising CoreError naming its slot, using
    the pattern already in the file.
  - a read-only `data` property is present per C3.1. It may raise CoreError
    for now; A-02 wires it.
  - the eight existing slots still exist and still behave exactly as before —
    the current test asserting the C30_SLOTS attribute set must pass
    UNCHANGED. This card is additive.
  - test_c31_attribute_set — asserts the six C3.1 names exist, transcribed
    from the contract into the test as a tuple. That tuple is the frozen
    api-facing surface from here on.
  - a comment above the two slot groups records why both exist: the eight are
    internal and predate the contract reading; the six are C3.1 and are what
    api/ may call.

Forbidden: removing, renaming or altering any of the eight existing slots;
  implementing any real service; importing anything from data/; editing
  anything under contracts/.

Stop and ask if: the C3.1 code block does not list six slots — report what it lists and stop.

Commit: A-01: add the six C3.1 slots to ServiceKit alongside the existing eight

Est. size: compose.py +40, tests +50. Total ~90.
