# PORTION P-03 — core: time + hashing

Goal: core/time.py and core/hash.py exactly per C1.5. Single source of
truth for "now" and for evidence integrity hashes.

Contract: 01-CONTRACTS-core.md (C1.5)

Files allowed: core/time.py · core/hash.py
              tests/core/test_time_hash.py

Context Pack: this card · 01-CONTRACTS-core.md C1.5 · core/errors.py
 SIGNATURES.

Done when:
 - now_utc() returns aware datetime; test: tzinfo UTC, and
   now_utc() > now_utc().replace(—¦) trivially — real check: two calls
   50 ms apart, second >= first (no regression, no zone drift).
 - sha256_bytes("hello".encode()) == hashlib.sha256("hello".encode()).hexdigest()
   (exact stdlib equality — property: core NEVER diverges from hashlib).
 - sha256_file: write a 0-byte file and a 50 MB random file (seeded
   os.urandom chunked), assert both match a streaming hashlib reference
   computed in the test.
 - short_id(): starts "i", 9 chars total, hex tail, two calls differ.
 - a4_page_pt constant present with frozen values.
 - `make gate` green — commit P-03 green.

Est. size: ~50 + ~60 lines, tests ~70.

"
      }
    ],
    "is_error": false,
    "command": "view",
    "path": "/workspace/P-03.md",
    "prev_exist": true,
    "old_content": null,
    "new_content": null,
    "kind": "FileEditorObservation"
  },
  "action_id": "4c9f160d-c7cd-4cdb-bb53-08d5f8490fd4",
  "kind": "ObservationEvent"
}