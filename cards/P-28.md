# PORTION P-28 — integration e2e: the whole story, real kit

Goal: tests/e2e/test_story.py — the acceptance story of
00-ARCHITECTURE, end to end, against the all-real kit from P-27. This test
IS the project's acceptance criteria as one executable narrative.

Contract: 00-ARCHITECTURE.md Acceptance section (transcribe it, do not paraphrase) · the P-19 loop test

Files allowed (R10 budget: 2 source files + their tests):
  TESTS    tests/e2e/test_story.py
  REGISTER tests/e2e/conftest.py (LINE ADD ONLY: the real-kit fixture —
           build_kit(WiredConfig(1,1,1)) on a FRESH tmp root, plus a
           scripted two-day PM fixture)

Context Pack: this card · 00-ARCHITECTURE acceptance section · the P-27 seam test
  (the kit it uses) · the SCOPE.md acceptance criteria.

Done when:
  test_acceptance_project_created — create_project produces the five
    world-objects (project row, charter event, open cycle, evidence dir,
    manifest.json). The P-10a assertion, re-proven across the whole stack.
  test_acceptance_evidence_story — attach xlsx, png and pdf: the timeline
    holds 3 EVIDENCE events in order, each with its purpose, and the test
    RECONSTRUCTS the narrative from the timeline query alone — one query
    answers the why, no search required.
  test_acceptance_loop_closed — open cycle, two actions (one carrying a
    defer signal) driven open to done, gate recorded PASSED, cycle closed;
    assert the cycle's event span covers its actions by date range.
  test_acceptance_report_is_truth — generate a report after the loop,
    parse the html, and assert the five sections carry LIVE data: the
    deferred action's purpose in red flags, the PASSED gate in status,
    prepared_for showing the empty placeholder.
  test_acceptance_backup_is_truth — backup, delete one file, restore,
    verify ok=True, and the evidence file's stored sha256 still matches
    the bytes on disk.
  test_acceptance_determinism — run the entire scenario twice in two fresh
    tmp roots; the final timeline, minus ids and created_at, is byte
    identical under a canonical JSON dump. This is the test that catches a
    non-deterministic sort, a wall-clock string, or an id leaking into a
    rendered field.

Forbidden: adding a route, service method or renderer field — this card
  READS and ASSEMBLES only. If the story is missing a piece, that is a
  card defect found here, and the fix is a CC card or a backfill to the
  owning P-card, never a new feature in this file.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-28: end-to-end acceptance story

Est. size: test_story ~260, conftest ~60. Total ~320.
