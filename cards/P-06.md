# PORTION P-06 — data: ProjectRepo + EventRepo + DataKit assembly

Goal: data/projects.py + data/events.py; DataKit now assembles
(projects, events) repos and the rest stays a typed slot that raises
"not implemented in this phase" (C2.1 shape is stable from P-06 — later
cards fill slots, never reshuffle).

Contract: 02-CONTRACTS-data.md (C2.1, C2.2 ProjectRepo + EventRepo)

Files allowed: data/projects.py · data/events.py
               tests/data/test_projects_events.py
               data/__init__.py (LINE EDITS: slot classes -> real)

Context Pack: this card · 02-CONTRACTS-data.md C2.1+C2.2 (those two
  sections) · core.sig.txt · data/db.py + data/rows.py SIGNATURES.

Done when:
  - Project: create -> get -> list order; code UNIQUE (second create
    'P001' raises DataError 'unique_violation'); get by unknown id/code
    raises UnknownProjectData (isinstance DataError AND code check).
  - set_status validates against enums (test both a good + a bad value —
    bad raises ServiceError code 'invalid_status').
  - Event: emit with ref_table 'actions' + ref_id; list_for returns
    occurred_at ASC, id ASC (test with 3 out-of-order inserts); list_for
    with kind filter; limit honored; 'note'/'charter' events allowed with
    ref_table None (I1 shape, test asserts both allowed forms).
  - DataKit(tmp): data.projects and data.events are the real classes
    (type assertion); data.cycles etc. still the placeholder — and a test
    confirms the placeholder raises CoreError not AttributeError.
  - single connection: DataKit holds ONE sqlite3.Connection (test
    inspects the attribute) — no repo opens its own handle (frozen rule,
    C2.1).
  - `make gate` green — commit P-06 green.

Est. size: projects ~110, events ~100, tests ~140."
      }
    ],
    "is_error": false,
    "command": "cat /workspace/P-06.md",
    "exit_code": 0,
    "timeout": false,
    "metadata": {
      "exit_code": 0,
      "pid": -1,
      "username": "openhands",
      "hostname": "e7a326b5633d",
      "working_dir": "/workspace/project/Post_IT-PM",
      "py_interpreter_path": "/usr/local/bin/python",
      "prefix": "",
      "suffix": "
[The command completed with exit code 0.]"
    },
    "full_output_save_dir": "/workspace/conversations/0d80ed143329449aa00d2d22faa89448/observations",
    "kind": "TerminalObservation"
  },
  "action_id": "e0053db0-ec65-4fca-8ec9-3281b33546c9",
  "kind": "ObservationEvent"
}