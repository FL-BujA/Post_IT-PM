# PORTION P-27 — THE SEAM: flip all three wired flags

Goal: app/root.py — build_kit(wired) returns the REAL kit for all three
flags (data, renderer, services). The stubs REMAIN in the suite. This must
pass the gate WITHOUT a single line of change to any test.

Contract: ALL four contract documents — every contract becomes real simultaneously here

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  app/root.py (the only source file — the seams are its
          if/else branches)
  TESTS   tests/app/test_seam.py

Context Pack: this card · all four contract documents, re-read rather than
  skimmed · every signature sheet the P-16..P-26b cards produced.

Done when:
  - test_seam: 8 WiredConfig combinations. For all-true, assert
    kit.data is data.DataKit, kit.project_svc is
    services.projects.ProjectSVC, kit.renderer is app.renderer.RenderKit
    (class assertions, not duck-typing). The other 7 combinations are a
    legal mix — the stubs are still present, so no combination can break.
  - the FULL suite (tests/contracts, tests/e2e, tests/api dual-kit) runs
    against build_kit(WiredConfig(data=1, renderer=1, services=1)) and is
    green. This is the MOSAIC Phase-3 acceptance: stubs pass, real passes,
    same tests, no exceptions.
  - if ANY test fails here: STOP. Do not edit the test. Record the failing
    test name and the ambiguous contract bullet in BUILD_STATE.md and open
    a CC proposal. Exposing the ambiguity at the cheapest moment is this
    card's designed behaviour.

Forbidden: editing ANY test in tests/contracts, tests/e2e or tests/api to
  make the real kit pass — that is the exact failure mode MOSAIC exists to
  prevent; weakening a contract to fit an implementation (that is a CC
  card with a rationale, never a quiet diff).

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-27: seam — all wired flags real

Est. size: root ~30, seam test ~120. Total ~150.
