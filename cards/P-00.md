# PORTION P-00 — Walking skeleton (Phase 1, MOSAIC Â§4)

Goal: the FULL system shape exists and runs end-to-end on stubs.
Layout, gate, composition root, all stubs, contract suite skeleton,
api shell, one e2e smoke. No real logic anywhere yet.

Contracts: ALL of blueprint/ (this bundle) — read C2.5, C3.6, C3.9 stub
specs exactly; they define the stub behavior.

Files allowed (skeleton exception: >2 files, this is the only such card):
 pyproject.toml  (deps + ruff config + mypy config; mypy strict on
                 app/ and core/, contracts live in docs so mypy target =
                 app core data services api)
 Makefile        (gate target per README Â§4)
 .gitignore
 config.sample.json
 cards/ (copy the card set into the repo? NO — cards stay in
         blueprint/cards; the repo gets a pointer in README)
 core/__init__.py        (empty stub module marker: __all__ = [])
 data/__init__.py        (empty stub marker)
 services/__init__.py    (empty stub marker)
 api/__init__.py         (empty stub marker)
 ui/index.html           ("<h1>PM Cockpit</h1>" placeholder)
 ui/prism.css            (:root token block placeholder, 6 tokens)
 ui/report/report.html.tmpl  (all {{tokens}}, skeleton classes only)
 ui/report/report.css    (skeleton)
 app/__init__.py         (def main(): raise NotImplementedError("P-26"))
 app/root.py             (build_kit(wired) with the THREE flags, returns
                          ServiceKit built ONLY from stubs for now)
 app/stubs/__init__.py
 app/stubs/data.py       (C2.5 canned data, deterministic)
 app/stubs/renderer.py   (C3.6 stub)
 app/stubs/services.py   (C3.9 canned behaviors)
 app/wired.py            (dataclass WiredConfig(data: bool=False,
                          renderer: bool=False, services: bool=False))
 tests/__init__.py
 tests/contracts/test_data.py      (parametrized IMPLS = [StubDataKit])
 tests/contracts/test_services.py  (IMPLS = [StubServiceKit])
 tests/contracts/test_renderer.py  (IMPLS = [StubRenderer])
 tests/e2e/test_smoke.py           (httpx ASGITransport against
                          create_app(StubKit): GET /api/health == 200,
                          body {"ok":true,...}; GET / == 200 html)
 api/app.py              (SKELETON ONLY: create_app(kit) wiring the
                          exception-handler envelope (C4.2) + GET
                          /api/health + static mount /static -> ../ui)
                          Routes are NOT implemented yet (P-22..P-24).

Context Pack: this card · blueprint/README.md · blueprint/00-*.md ·
 blueprint/02-*.md C2.5 · blueprint/03-*.md C3.6+C3.9 · blueprint/04-*.md
 C4.0+C4.2 (envelope shape only).

Done when:
 - `make gate` green (ruff format --check, ruff check, mypy, pytest).
 - smoke e2e green; `uv run pytest -q` run twice in same state = same
   result (determinism check).
 - app/root.py: build_kit(WiredConfig(data=1, renderer=1, services=1))
   still returns the STUB kit in P-00 (flags read, real classes not
   imported yet) — the flag mechanism exists and is tested (a test
   asserts stubs are returned for every flag combo at this phase).
 - committed + tagged: P-00 skeleton-green
EXIT PHASE 1 GATE (MOSAIC Â§4): all four above + BUILD_STATE.md updated.

Forbidden: any real repository, any SQLite call outside tests, importing
fastapi outside api/, new dependencies not in pyproject pin list.

Est. size: ~550 lines total across scaffolding.

"
      }
    ],
    "is_error": false,
    "command": "view",
    "path": "/workspace/P-00.md",
    "prev_exist": true,
    "old_content": null,
    "new_content": null,
    "kind": "FileEditorObservation"
  },
  "action_id": "f44354ce-937c-41b3-bca3-8c2aee9849b2",
  "kind": "ObservationEvent"
}