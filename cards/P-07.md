# PORTION P-07 — data: GateRepo + CycleRepo (invariant I3 lives here)

Goal: data/gates.py + data/cycles.py. The loop's structural lock: a cycle
cannot be recorded closed without a gate outcome.

Contract: 02-CONTRACTS-data.md (C2.2 GateRepo, CycleRepo)

Files allowed: data/gates.py · data/cycles.py
              tests/data/test_gates_cycles.py

Context Pack: this card · 02-CONTRACTS-data.md (those two sections) ·
 core.sig.txt · data/rows.py SIGNATURES · data/projects.py SIGNATURES.

Done when:
 - Gate lifecycle: create (planned_date NULL allowed) -> record_outcome(
   PASSED) sets outcome + actual_date + emits a GATE event (ref_table
   'gates', ref_id gate id) — event asserted by reading timeline_events.
 - record_outcome on an already-outcomed gate: raises ServiceError
   code 'gate_already_outcomed' (frozen: idempotence is NOT silent —
   a second outcome is a data-integrity event, the PM must see it).
 - close_cycle(gate PASSED) -> closed_at set, gate linked, cycle's
   GATE event present, cycle row returned.
 - close_cycle on a PLANNED gate -> GateMissing raised, cycle UNCHANGED
   (closed_at still NULL, gate_id still NULL) — the I3 test, named as
   test_i3_close_requires_outcome in the file (the invariant gets a
   named test, so a regression fails loudly by name).
 - close_cycle with a gate from ANOTHER project -> GateMissing.
 - current_for: one open cycle + one closed cycle -> returns the open
   one; none open -> None.
 - open() emits a PHASE event 'Cycle opened: <name>'.
 - `make gate` green — commit P-07 green.

Est. size: gates ~90, cycles ~110, tests ~160.

"
      }
    ],
    "is_error": false,
    "command": "view",
    "path": "/workspace/P-07.md",
    "prev_exist": true,
    "old_content": null,
    "new_content": null,
    "kind": "FileEditorObservation"
  },
  "action_id": "4aa77909-ad06-49f7-a6db-cf534767efd1",
  "kind": "ObservationEvent"
}