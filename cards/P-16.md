# PORTION P-16 — renderer: report HTML (tokens contract, C4.6)

Goal: app/renderer.py implementing C3.6 RenderService — the first real
renderer into the contract suite. Renders report.html.tmpl with the exact
token set from C4.6.

Contract: 03-CONTRACTS-services.md (C3.6) · 04-CONTRACTS-api.md (C4.6 token list)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   app/renderer.py
  TESTS    tests/renderer/test_renderer.py
  REGISTER tests/contracts/test_renderer.py (LINE EDIT ONLY: append
           RealRenderer to the IMPLS list)

Context Pack: this card · C3.6 in full · C4.6 in full (transcribe the token list
  into the test as data) · ui/report/report.html.tmpl current state ·
  core types SIGNATURES.

Done when:
  - render(ReportArtifact): parse the output with stdlib html.parser and
    assert every C4.6 token was replaced — no '{{' remains anywhere.
  - all seven sections present: target, status, priorities, red flags,
    escalation, prepared_for, engagement strip (assert each section id or
    class exists).
  - test_prepared_for_empty — prepared_for == '' renders the frozen
    placeholder text (C3.6 acceptance bullet).
  - red flags renders the 14-day deferred/reopen list: a 5-signal fixture
    where 3 qualify, assert exactly those 3 in severity order.
  - deterministic: the same rows in produce byte-identical HTML out (two
    calls compared) — the C3.6 contract property.
  - token discipline: every var(...) used in report.css and in the
    rendered output is in the PRISM token list (assert by exclusion).
  - the contract suite is green with BOTH renderers — StubRenderer still
    passes, so the seam stays honest.

Forbidden: raw hex colours anywhere in the template or the renderer;
  changing the C4.6 token names to fit the template (that is a CC card);
  emitting any event — the renderer adds none.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-16: report HTML renderer and token contract

Est. size: renderer ~180, tests ~180. Total ~360.
