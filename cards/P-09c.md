# PORTION P-09c — data: integrity (I2) + DataKit closure

Goal: data/integrity.py — the I2 machinery — plus the final slot fill
in data/__init__.py that closes the C2.1 DataKit attribute set.

Contract: 02-CONTRACTS-data.md (C2.2 integrity section, C2.1 DataKit shape)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   data/integrity.py
  TESTS    tests/data/test_integrity.py
  REGISTER data/__init__.py (LINE EDITS ONLY: fill the remaining slots;
           no logic, no more than one line per slot)

Context Pack: this card · 02-CONTRACTS-data.md (integrity section + C2.1 attribute
  list) · core.sig.txt (hash signatures) · data/evidence.py SIGNATURES.

Done when:
  - test_i2_missing_file_flagged — a db row whose file has been deleted:
    verify returns ok=False, the row appears in missing[], and NOTHING is
    deleted from the db (assert the row count is unchanged before/after).
  - test_i2_hash_mismatch — file bytes changed after record: mismatched[]
    is non-empty and names the rel_path.
  - test_i2_orphan — a file on disk with no db row: orphans[] contains
    its rel_path.
  - test_i2_clean — a consistent workspace: ok=True and all three lists
    are empty.
  - DataKit exposes every C2.1 slot as a real implementation: one test
    asserts the full attribute set and the type of each — DATA CONTRACT
    CLOSED.

Forbidden: deleting, repairing, or quarantining anything during verify
  (I2 reports, it never mutates — a test asserts the db row count and the
  file count are both unchanged across a verify call); adding logic to
  data/__init__.py beyond slot assignment.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-09c: integrity (I2) and DataKit closure

Est. size: integrity ~130, tests ~130. Total ~260.
