# PORTION P-10 — services: composition, project & phase lifecycle

Goal: services/compose.py (ServiceKit assembly) + services/projects.py
(create_project full glue, update_project), services/phase.py
(open_cycle, close_cycle with the I3 service-side check before it even
reaches data).

Contract: 03-CONTRACTS-services.md (C3.0, C3.1 project/phase sections)

Files allowed: services/compose.py · services/projects.py · services/phase.py
               tests/svc/test_projects_phase.py
               services/__init__.py (LINE EDITS: compose re-export)

Context Pack: this card · 03-CONTRACTS-services.md C3.0+C3.1 (project+phase
  parts) · core.sig.txt · C2.1 DataKit shape + rows SIGNATURES.

Done when:
  - create_project("Alpha Bom", "2026-09-30", "TBD") on a FRESH empty
    workspace (tmp) produces, in ONE call, verified by the test:
      * project row status 'planned'
      * charter event present (kind 'charter', summary contains 'Alpha Bom')
      * first open cycle row ('Charter cycle')
      * evidence/ + reports/ + backups/ dirs created
      * root manifest.json exists with projects: ["P001"]
      * NO evidence rows (nothing attached yet)
    — this is the P-10 signature test: the "one call, whole world"
    acceptance (00-ARCHITECTURE core acceptance, project side).
  - duplicate code -> ServiceError 'project_exists'.
  - close_cycle: open -> gate 'acceptance' recorded PASSED ->
    close_cycle -> cycle closed, GATE event present (I3, service side).
  - close_cycle with NO gate -> GateMissing, cycle still open (service-
    side guard BEFORE data call — test patches data.close_cycle with a
    spy: it must not be called when the gate is missing).
  - update_project: name/target/prepared_for each persist + emit UPDATE
    event with the changed field in the summary (parametrized x3).
  - ServiceKit(tmp) exposes project_svc, phase_svc, and placeholder slots
    (actions/evidence/minutes/report/backup/integrity) that raise
    CoreError until their card (typed slot classes — same pattern as
    data's, so C3.0's attribute set is stable from P-10).
  - `make gate` green → commit P-10 green.

Est. size: compose ~90, projects ~140, phase ~110, tests ~180.
