# PORTION P-02 — core: paths, slugs, value objects

Goal: core/paths.py and core/values.py exactly per C1.2 + C1.3. These two
files ARE the glue rule, machine-enforced; later layers call them, never
re-implement validation.

Contract: 01-CONTRACTS-core.md (C1.2, C1.3)

Files allowed: core/paths.py · core/values.py
              tests/core/test_paths_values.py
              core/__init__.py (LINE EDITS: re-export new names)

Context Pack: this card · 01-CONTRACTS-core.md · core/enums.py +
 core/errors.py SIGNATURES (not bodies) from P-01.

Done when:
 - slugify("Bom (v3) - final", date, ext) == "<date>_bom_v3_final.<ext>"
   (exact test from contract), plus: empty raises InvalidSlug; 100-char
   stem truncates to 40; unicode -> ascii-safe.
 - normalize_relpath PASS cases (exact): 'evidence/P001/2026-07-14_bom.xlsx',
   'reports/P001/2026-08-05_report.html';
   FAIL cases (each with the EXACT exception class + .code asserted):
   '../x' (PathEscape), 'C:/x' (PathEscape), '/x' (PathEscape),
   'evidence/P9/2026-01-01_a.pdf' (InvalidSlug — bad code),
   'evidence/P001/no_date.pdf' (InvalidSlug — missing date prefix).
 - Owner("  ana  ") == Owner("ana"); Owner("") raises OwnerError.
 - PreparedFor("" ) raises; PreparedFor("TBD") ok.
 - EventRef: ref_table None implies ref_id None (constructor enforces);
   ref_table must be in the frozen 5-table set.
 - `make gate` green — commit P-02 green.

Forbidden: pathlib.Path() calls inside (pure string in/out — the string
 IS the stored form); regex that accepts backslashes.

Est. size: ~120 + ~70 lines, tests ~110.

"
      }
    ],
    "is_error": false,
    "command": "view",
    "path": "/workspace/P-02.md",
    "prev_exist": true,
    "old_content": null,
    "new_content": null,
    "kind": "FileEditorObservation"
  },
  "action_id": "98d12861-5f71-49ba-8dc9-95d0767b0552",
  "kind": "ObservationEvent"
}