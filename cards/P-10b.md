# PORTION P-10b — services: phase lifecycle (I3 service-side guard)

Goal: services/phase.py — open_cycle and close_cycle, with the I3 gate
check performed in the service BEFORE the data call is reached.

Contract: 03-CONTRACTS-services.md (C3.1 phase section)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   services/phase.py
  TESTS    tests/svc/test_phase.py
  REGISTER services/__init__.py (LINE EDIT ONLY: phase slot -> real)

Context Pack: this card · 03-CONTRACTS-services.md C3.1 phase part ONLY ·
  core.sig.txt (GateOutcome) · services/compose.py SIGNATURES from P-10a ·
  data cycles/gates SIGNATURES.

Done when:
  - open_cycle creates an open cycle row for the project and emits its
    event (fixture project from the P-10a service, one assertion each).
  - close_cycle happy path: open cycle -> gate 'acceptance' recorded
    PASSED -> close_cycle -> the cycle row is closed and a GATE event is
    present (I3, service side).
  - test_i3_guard_before_data — close_cycle with NO gate raises GateMissing
    and the cycle remains open. The test patches data.close_cycle with a
    spy and asserts the spy was NEVER called: the guard runs before the
    data layer is reached, not after it refuses.
  - close_cycle on an already-closed cycle raises ServiceError (not a
    silent no-op).

Forbidden: duplicating the gate rules in this layer if they already exist
  in core (import them); calling data.close_cycle before the gate check;
  creating projects (that is P-10a).

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-10b: phase lifecycle with I3 service-side guard

Est. size: phase ~110, tests ~120. Total ~230.
