# PORTION P-23 — ui: board view (actions + priorities)

Goal: ui/views/board.html and board.js — the priorities board for the
team meeting: action cards in three columns, priority badges, owner chips,
reopen count visible.

Contract: C1 BoardRow fields (columns are SERVER-derived — the view renders the row, it does not bucket)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   ui/views/board.html
  SOURCE   ui/views/board.js
  TESTS    tests/ui/test_board.py (Playwright)
  REGISTER ui/index.html (LINE EDIT ONLY: mount the view)

Context Pack: this card · C1 BoardRow (transcribe into the fixture) · the shell
  SIGNATURES from P-21b · the PUT state route SIGNATURE from P-19.

Done when:
  - a fixture of 12 actions, 4 per column, renders 3 columns with exact
    counts. P1-P3 carry the "critical" badge class, P4-P5 "normal".
  - dragging a card from open to in_progress fires the PUT (intercepted by
    the test), the card moves, and server order is preserved. Releasing
    outside a column returns the card to its origin and fires ZERO PUTs.
  - an action with reopen_count 1 shows the reopen badge with its count.
  - an empty column renders its frozen empty copy.
  - a failed PUT rolls the card back to its origin column and shows an
    error toast (the board is server-truth; an offline "stick" is a bug).

Forbidden: auto-sorting by priority inside the view (board order is
  insertion order — the PM arranges the story); bucketing client-side;
  any localStorage write beyond the frozen project key.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-23: board view

Est. size: board.html ~160, board.js ~140, tests ~200. Total ~500.
