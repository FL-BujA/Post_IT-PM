# PORTION P-21a — ui: PRISM tokens + app shell markup

Goal: ui/prism.css (the real token set) and ui/index.html (the app
shell: project switcher, six view containers, nav). Markup and tokens
only — the behaviour lands in P-21b.

Contract: 04-CONTRACTS-api.md (C4.7 state rule) · 00-ARCHITECTURE (UI layer row) · PRISM_UI_Protocol

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  ui/prism.css
  SOURCE  ui/index.html
  TESTS   tests/ui/test_shell_markup.py (Playwright, headless)

Context Pack: this card · C4.7 · the SCOPE.md requirements that bind the shell
  (transcribe only those) · the PRISM token list if delivered; if it has
  not been delivered, build the fallback light/dark set and log the
  deviation in BUILD_STATE.md.

Done when:
  - loading index.html against the P-00 stub app shows six nav items:
    timeline, board, meetings, evidence, report, engagement.
  - all six view containers exist in the DOM with their frozen ids.
  - the page loads with an empty console error list (assert).
  - CSS audit: no raw hex colour appears in index.html or prism.css
    outside the :root token block (regex assertion over both files —
    PRISM means tokens, not values).
  - the dark theme class toggles the token set (assert a computed colour
    changes when the class is applied; persistence is P-21b).

Forbidden: any JavaScript beyond a single module script tag pointing at
  app.js (which does not exist until P-21b); a second stylesheet for the
  shell; any charting library or <canvas> element.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-21a: PRISM tokens and app shell markup

Est. size: prism.css ~180, index.html ~120, tests ~90. Total ~390.
