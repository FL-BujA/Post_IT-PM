# PORTION P-14 — services: search + integrity

Goal: services/search.py (search with project filter and top-3) and
services/integrity.py (verify delegating to data.Integrity).

Contract: 03-CONTRACTS-services.md (C3.7, C3.8 verify part)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   services/search.py
  SOURCE   services/integrity.py
  TESTS    tests/svc/test_search_integrity.py
  REGISTER services/__init__.py (LINE EDITS ONLY: two slots -> real)

Context Pack: this card · 03-CONTRACTS-services.md C3.7 + C3.8 verify part ·
  C2.2 Search and Integrity SIGNATURES.

Done when:
  - search('BOM', project 'P001') returns only P001 hits — the fixture has
    the same token in two projects, and the filter is the point of the test.
  - result.top3 is non-empty when hits exist: a 5-hit fixture, assert the
    exact id sequence in rank order.
  - verify() on a clean workspace returns a VerificationReport with
    ok=True, and the test asserts the report came from the data layer, not
    from app/stubs (class identity assertion).
  - verify() with a deleted file: missing[] contains the rel_path, ok is
    False, and the db is untouched (row count stable) — I2 at the service
    level, in one call.

Forbidden: re-ranking or re-sorting search results in this layer;
  repairing anything during verify.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-14: search and integrity services

Est. size: search ~70, integrity ~70, tests ~120. Total ~260.
