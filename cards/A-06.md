# PORTION A-06 — data: backfill GateRepo.list_for

Goal: C2.2 specifies GateRepo as:

    create(project_id, name, planned_date=None, exit_criteria="", notes="")
    record_outcome(gate_id, outcome: GateOutcome, actual_date=None)
    get(gate_id) -> GateRow
    list_for(project_id) -> list[GateRow]

The built GateRepo has create, get and record_outcome. list_for was never
implemented, so there is no way to retrieve a project's gates at all. C3.3
list_for_project needs open gates for its snapshot and cannot be built
without it.

This card adds list_for ONLY. The create and record_outcome signatures also
differ from C2.2 (the contract has exit_criteria, notes and actual_date, and
different return types); those are NOT in scope here — they are recorded in
BUILD_STATE.md as an open divergence and reconciled in their own card, because
changing them would break every caller built so far.

Contract: 02-CONTRACTS-data.md (C2.2 GateRepo)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  data/gates.py
  TESTS   tests/data/test_gates_cycles.py (extend; do not rewrite)

Context Pack: this card · the C2.2 GateRepo block of contracts/02-CONTRACTS-data.md ·
  data/gates.py, in Files allowed — the other repos' list_for methods are the
  pattern, and one is already in this file's sibling CycleRepo. core.sig.txt
  for GateRow's fields. Do not open any other .py file.

Done when:
  - GateRepo.list_for(project_code) returns list[GateRow] for that project.
    Note the parameter: C2.2 writes project_id as shorthand, and every
    implemented repo uses project_code — follow the code convention, as the
    other list_for methods in data/ do.
  - ordering is planned_date ascending, then id ascending for gates with no
    planned_date. A fixture of four gates with mixed planned_date (two set,
    two NULL) asserts the exact id sequence.
  - an unknown project_code returns an empty list, it does not raise. A test
    asserts this — the snapshot caller needs an empty list, not an exception.
  - a project with no gates returns an empty list.
  - every existing test in tests/data/test_gates_cycles.py still passes with
    its intent intact.
  - core.sig.txt regenerated (python3 tools/sigsheet.py) and `make gate`
    green, so list_for is in the sheet before A-03 uses it.

Forbidden: changing create, get or record_outcome — their divergence from
  C2.2 is real but out of scope and would break existing callers; adding a
  filter parameter to list_for that C2.2 does not name (the snapshot filters
  for open gates itself); editing anything under contracts/.

Stop and ask if: GateRow's fields in core.sig.txt do not include planned_date, or data/gates.py already has a list_for. Report which and stop.

Commit: A-06: backfill GateRepo.list_for per C2.2

Est. size: gates.py +35, tests +70. Total ~105.
