# PORTION P-04 — core: final public surface (contract closure)

Goal: core/__init__.py re-exports the EXACT public API of core (one
surface, ≤ 7 names per source module, R10) and the core contract suite
closes: every acceptance bullet in 00-ARCHITECTURE.md "core" section is
encoded as a green test reachable via import core.

Contract: 01-CONTRACTS-core.md (all)

Files allowed: core/__init__.py
               tests/core/test_package_surface.py

Context Pack: this card · 01-CONTRACTS-core.md · SIGNATURES of all six
  core modules (generated: grep "def \|class " core/*.py > core.sig.txt —
  build that signature sheet as part of this card, commit it, and use it
  as the pack artifact from here on — MOSAIC §6 signature sheets).

Done when:
  - the signature sheet core.sig.txt exists and is committed.
  - test_package_surface: import core; every name listed in the contract's
    "Public API" statements is accessible as core.X; and core has NO
    attribute referencing a file path outside the core package (a test
    walks core.__dict__ and asserts no value contains "D:" after
    os.path.normcase — cheap insurance against glue-rule leaks).
  - public API count per module ≤ 7 (test counts __all__ if present,
    else module-level defs; document in the test).
  - `make gate` green → commit P-04 green → CORE CONTRACT CLOSED.

Est. size: ~40 + tests ~60.
