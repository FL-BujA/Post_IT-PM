# PORTION P-05b — data: move meeting_minutes and report_history DDL into the migration

Goal: Complete what P-05a started: meeting_minutes and report_history are
created by _ensure_table methods inside MinutesRepo and ReportHistoryRepo.
Move both statements into data/migrate.py and delete the methods.

As in P-05a this is a move. Both table definitions are correct and are copied
verbatim. C2.0 names the table meeting_minutes — that is the correct name and
it must not be shortened.

Contract: 02-CONTRACTS-data.md (C2.0 table list; C2.4 migration)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  data/minutes.py
  SOURCE  data/reports_history.py
  TESTS   tests/data/test_migrate.py
  REGISTER data/migrate.py (ADD ONLY: the two CREATE TABLE statements,
           copied verbatim from the two repos — no other change)

Context Pack: this card · the C2.0 table list of contracts/02-CONTRACTS-data.md ·
  data/minutes.py and data/reports_history.py, both in Files allowed, which
  carry the DDL to move · the engagement_signals block that P-05a added to
  data/migrate.py, as the pattern to follow. Do not open any other .py file.

Done when:
  - data/migrate.py creates meeting_minutes and report_history with EXACTLY
    the columns, types, constraints and defaults currently declared in the two
    repositories. Copy the statements; do not rewrite them.
  - both _ensure_table methods are deleted, along with the calls to them in
    each repository's __init__. Nothing else in either repository changes.
  - test_migrate asserts both tables exist after migrate() on a fresh db, with
    their column lists read from PRAGMA table_info and compared to the C2.0
    column names — the same assertion style P-05a used.
  - migrate() remains idempotent: running it twice on the same db succeeds and
    still returns 1 (an existing test covers this; it must stay green).
  - the full suite is green, and the P-09a and P-09b tests pass UNCHANGED.
  - after this card, data/migrate.py creates every table C2.0 defines. One
    test asserts the table count from sqlite_master equals the C2.0 count, so
    a future table cannot be added outside the migration without failing.

Forbidden: renaming meeting_minutes to minutes or renaming any column;
  changing a type, constraint or default while moving the statement; editing
  anything under contracts/; touching data/actions.py; changing any repository
  method other than removing the _ensure_table call from __init__.

Stop and ask if: the engagement_signals block from P-05a is not present in data/migrate.py — that means P-05a has not landed. Stop; do not add all three tables here.

Commit: P-05b: move meeting_minutes and report_history DDL into migrate.py

Est. size: migrate.py +35, minutes.py -18, reports_history.py -18, tests +35. Total ~106.
