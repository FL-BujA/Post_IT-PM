# PORTION P-29 — handover: README, HANDOVER doc, clean-clone proof

Goal: The PM's single artifact for "the software is done": the final
README, docs/HANDOVER.md, and a clean-clone build proof — the tool is
handover-ready only if a fresh clone builds green.

Contract: 00-ARCHITECTURE.md (what it does, invariants I1-I6) · SCOPE.md handover section · the P-27 seam and P-28 integration

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   README.md (overwrite: final version)
  SOURCE   docs/HANDOVER.md
  TESTS    tests/handover/test_clone_build.py
  REGISTER contracts/BUILD_STATE.md (LINE ADD ONLY: the closing summary)

Context Pack: this card · 00-ARCHITECTURE · the SCOPE.md handover section · every
  card's BUILD_STATE.md entry — the log is the pack, and the handover doc
  must match it.

Done when:
  - the README opens with the 30-second story: what it is, who it is for,
    and the one command to run it. A test asserts the "Daily use" section
    contains the EXACT run command by string match against app/main.py's
    module docstring. Drift here is a handover defect, not a nit.
  - docs/HANDOVER.md names all six invariants I1-I6 with a "how it is
    enforced" line for each; a test asserts all six names appear.
  - test_clone_build — a FRESH clone (git clone file://... into tmp, no
    local state, no .venv) runs `make gate` under subprocess with a 600s
    timeout, exits 0, and the passing set includes tests/app/test_seam.py
    and tests/e2e/test_story.py. A dirty working copy that passes locally
    is not a handover; a green clone is.
  - the dependency count is unchanged from P-17 (assert against
    pyproject.toml — the tool is done growing dependencies).
  - BUILD_STATE.md closes with the completion line naming the final card
    count, the seam status, the clone status and the determinism status,
    and a test asserts that line is present.

Forbidden: any new dependency (asserted); documenting a feature that is
  not in a card — the handover doc lists WHAT IS; declaring handover with
  the seam not green (the clone-build test fails the handover if the seam
  test is absent from the passing set).

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-29: handover documentation and clean-clone proof

Est. size: README ~120, HANDOVER ~180, tests ~90. Total ~390.
