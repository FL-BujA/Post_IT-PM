# PORTION P-26a — app: config + window adapter

Goal: app/config.py (read/create config.json, the folder memory at tool
level) and app/window.py (the window(url) adapter, whose implementation is
pywebview or the Edge fallback — the CONTRACT is the window, not the
library).

Contract: C3.0 · C4.5 (port from config.json)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   app/config.py
  SOURCE   app/window.py
  TESTS    tests/app/test_config_window.py
  REGISTER pyproject.toml (LINE EDIT ONLY: pin the window dependency; if
           R1 failed at M0, pin nothing and log the fallback deviation)

Context Pack: this card · C3.0 · C4.5 · core/paths.py SIGNATURES (root default).

Done when:
  - config.read on a FRESH tmp dir with no config.json creates one whose
    root is that tmp dir; a second read returns the same root without
    rewriting the file (persistence — the PM's folder survives a restart).
  - config.read on an EXISTING config.json returns its root and port
    unchanged.
  - a config naming a non-loopback bind address raises ConfigError. This
    is a security invariant of a local tool and the test asserts refusal.
  - window(url) is called with the url and returns without raising under
    the test's fake adapter. The test calls the ADAPTER only, never the
    library, so the R1 outcome never leaks into another card.

Forbidden: starting a server here; importing the window library at module
  import time (import it inside the adapter so tests never need it).

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-26a: config and window adapter

Est. size: config ~80, window ~60, tests ~100. Total ~240.
