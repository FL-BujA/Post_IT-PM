# PORTION P-09e — data: MinutesRepo and ReportHistoryRepo return typed rows

Goal: Complete the backfill started in P-09d: MinutesRepo.list_for and
ReportHistoryRepo.list_for return their contract row types instead of
list[tuple]. The row classes already exist from P-09d — this card only changes
the two repositories.

Contract: 02-CONTRACTS-data.md (C2.2 MinutesRepo, C2.2 ReportHistoryRepo)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  data/minutes.py
  SOURCE  data/reports_history.py
  TESTS   tests/data/test_comms.py and tests/data/test_reports_search.py
          (update the minutes and report-history assertions)

Context Pack: this card · the C2.2 MinutesRepo and ReportHistoryRepo sections of
  contracts/02-CONTRACTS-data.md · core.sig.txt, which now carries MinutesRow
  and ReportRow with their full field lists from P-09d. Do not open
  data/rows.py — its content is in core.sig.txt.

Done when:
  - MinutesRepo.list_for returns list[MinutesRow]; every field populated.
  - ReportHistoryRepo.list_for returns list[ReportRow]; every field populated.
  - both add() methods still return int, per contract — unchanged.
  - the existing minutes and report-history tests are updated to read
    attributes rather than unpack tuples, and every assertion they made before
    still holds — including the generated_at DESC ordering and the verbatim
    minutes_text round-trip.
  - test_row_types_match_tables — one test per repo asserting the row's field
    names equal that table's column names in declaration order, read from the
    schema, mirroring the test P-09d added for signals.
  - no repository outside these two is modified.

Forbidden: editing data/rows.py (P-09d owns it — if a row field is wrong,
  stop and say so rather than fixing it here); editing anything under
  contracts/; changing method names, parameters or return types other than the
  two list_for methods named above.

Stop and ask if: MinutesRow or ReportRow is absent from core.sig.txt — that means P-09d has not landed or the sheet is stale. Stop; do not create the row classes here.

Commit: P-09e: MinutesRepo and ReportHistoryRepo return typed rows

Est. size: minutes.py ~15 changed, reports_history.py ~15 changed, tests ~70 changed. Total ~100.
