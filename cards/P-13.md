# PORTION P-13 — services: meetings + report generation

Goal: services/meetings.py (add_minutes, list) and services/report.py
(generate_report through the renderer slot). The renderer is still the
stub here; the token contract is exercised by P-16.

Contract: 03-CONTRACTS-services.md (C3.5, C3.6 service side)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   services/meetings.py
  SOURCE   services/report.py
  TESTS    tests/svc/test_minutes_report.py
  REGISTER services/__init__.py (LINE EDITS ONLY: two slots -> real)

Context Pack: this card · 03-CONTRACTS-services.md meetings + report sections ·
  C3.6 renderer signature · core.sig.txt.

Done when:
  - add_minutes writes the minutes row, emits a MEETING event, and the
    text is findable through search (a unique token round-trips —
    the cross-layer minutes -> FTS assertion).
  - list_min for the project is ordered started_at DESC and honours limit.
  - generate_report('P001') against the stub renderer:
      * ReportArtifact.html_path exists under reports/<code>/ with the
        name '<yyyymmdd>_<slug>.html' (exact format assertion)
      * a report_history row and a REPORT event with ref reports/<id>
      * the service calls the renderer slot EXACTLY ONCE (spy test — this
        must still pass unchanged when the real renderer lands in P-16)

Forbidden: any HTML string in this layer (it belongs to the renderer);
  reading evidence files — the report reads DB rows only; the renderer may
  stream files, the service never does.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-13: meetings and report generation

Est. size: meetings ~80, report ~110, tests ~140. Total ~330.
