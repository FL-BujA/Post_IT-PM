# PORTION P-18 — api: project + phase routes

Goal: api/routes_projects.py and api/routes_phases.py implementing the
C4 routes for project CRUD, charter, cycle open/close and gates. The
exception envelope is now exercised by real routes.

Contract: 04-CONTRACTS-api.md (route table: project and phase rows; C4.1, C4.2, C4.5 envelope rules)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   api/routes_projects.py
  SOURCE   api/routes_phases.py
  TESTS    tests/api/test_projects_phase_routes.py
  REGISTER api/app.py (LINE EDITS ONLY: include the two routers)

Context Pack: this card · 04-CONTRACTS-api.md C4.0-C4.2 and the project/phase rows
  of the route table ONLY · C3.1 service SIGNATURES · the httpx ASGI
  fixture from tests/conftest (P-00).

Done when:
  - POST /projects returns 201 with a ProjectRow dict (exact key set).
    POST with the same code again returns 409 {code:'project_exists'} and
    the envelope has EXACTLY 4 keys (set equality assertion).
  - GET /projects/{code} for an unknown code returns 404
    {code:'unknown_project'} with a message containing 'P999' — the
    404-names-the-missing-entity rule (C4.2).
  - GET /projects returns 200, an ordered array, length equal to fixture.
  - POST /projects/{code}/cycles, POST .../gates, POST .../gates/{id}/
    outcome and POST .../cycles/{id}/close each return their frozen body
    shape. Closing a cycle WITHOUT an outcome returns 409
    {code:'gate_missing'} — I3 at the HTTP boundary.
  - PATCH /projects/{code} returns 200 with the updated row.
  - all bodies are JSON: Content-Type asserted on 5 sampled calls.

Forbidden: HTML in any response body; a handler returning a non-frozen
  dict shape; catching an exception and re-raising it without the envelope.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-18: project and phase routes

Est. size: routes ~160 + ~90, tests ~200. Total ~450. If either route file exceeds 300 lines, emit SPLIT.
