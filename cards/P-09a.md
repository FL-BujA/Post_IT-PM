# PORTION P-09a — data: minutes + signals

Goal: data/minutes.py and data/signals.py — the two comms slots of
DataKit. Minutes storage must be verbatim and FTS-visible; signals carry
the frozen 'Signal #<id>' summary format.

Contract: 02-CONTRACTS-data.md (C2.2 minutes section, C2.2 signals section)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE  data/minutes.py
  SOURCE  data/signals.py
  TESTS   tests/data/test_comms.py

Context Pack: this card · 02-CONTRACTS-data.md (minutes + signals sections ONLY) ·
  core.sig.txt · data/rows.py SIGNATURES (not bodies).

Done when:
  - minutes.add emits a MEETING event and stores minutes_text verbatim
    (test writes a string containing '**bold**' and reads back the exact
    bytes — no normalisation, no markdown handling).
  - minutes.add stores the unique token 'quartz-cinder' when given it;
    this card asserts STORAGE only. The search assertion for that token
    belongs to P-09b — do not add a search test here.
  - signals.insert emits a SIGNAL event whose summary starts with
    'Signal #' (test asserts the prefix, the frozen format).
  - signals.list_for filters: kind, owner, and resolved each tested
    independently (three assertions, one fixture).
  - signals.set_resolved toggles resolved and sets resolved_at
    (test asserts resolved_at is None before and non-None after).

Forbidden: touching data/__init__.py (the final slot fill is P-09c);
  implementing search or FTS here; resolving a signal as a side effect of
  any other call (signals are append-only, only set_resolved mutates).

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-09a: minutes + signals repositories

Est. size: minutes ~80, signals ~90, tests ~140. Total ~310.
