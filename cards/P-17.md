# PORTION P-17 — renderer: PDF export (A4 at 72dpi)

Goal: PDF export behind the same C3.6 surface: the renderer gains
to_pdf(html_path). The token contract does not change — same HTML, second
output. The contract does not name the library; this card does, so a swap
is a one-card change.

Contract: 03-CONTRACTS-services.md (C3.6 — to_pdf)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   app/renderer_pdf.py
  TESTS    tests/renderer/test_pdf.py
  TESTS    tests/renderer/conftest.py
  REGISTER pyproject.toml (LINE EDIT ONLY: add the ONE pdf dependency,
           pinned — R11)

Context Pack: this card · C3.6 · app/renderer.py SIGNATURES from P-16.

Done when:
  - render_to_pdf(html) returns a ReportArtifact with pdf_path set, the
    file begins with the magic bytes b'%PDF', and its size exceeds 4KB.
  - page discipline: count pages from the pdf bytes (/Type /Page count is
    acceptable for this contract) and assert 2 or fewer. Three or more
    fails the gate. This card owns the page math; aesthetics are not here.
  - deterministic page count: render the same html three times, compare.
  - the renderer emits NO events: assert the event count is stable across
    a render call (C3.6 says so; report_history is the service's job).

Forbidden: new page-break CSS without a comment citing the A4 at 72pt
  budget; any dependency beyond the ONE allowed; changing the HTML the
  renderer produces (that is P-16's contract).

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-17: PDF export

Est. size: renderer_pdf ~120, tests ~90. Total ~210.
