# PORTION P-25b — ui: engagement view

Goal: ui/views/engagement.html and engagement.js — per-owner signal
counts (defer, extension, late start, reopen) and the watch chip the PM
uses to spot engagement issues.

Contract: C3.1 signals · C1 SignalKind (counts are SERVER-derived)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   ui/views/engagement.html
  SOURCE   ui/views/engagement.js
  TESTS    tests/ui/test_engagement.py (Playwright)
  REGISTER ui/index.html (LINE EDIT ONLY: mount the view)

Context Pack: this card · C3.1 signals row · C1 enums. The four labels are FROZEN
  here — defer to "deferred", extension_request to "asked for extension",
  late_start to "late start", reopen to "reopened". Transcribe these four
  strings into the test.

Done when:
  - a fixture of 9 signals across 3 owners renders 3 owner chips with
    per-kind counts: assert the exact 3x4 matrix, transcribed from the
    fixture.
  - an owner with 2 or more reopens carries the "watch" chip class, so the
    P-11 signal row and this chip are the same truth joined by the owner
    string.
  - the four kind labels render exactly the frozen strings (assert each).
  - an empty engagement view renders its frozen copy.

Forbidden: a fifth signal label (a new kind is a CC card, not a UI
  addition); computing counts client-side; any DELETE call bound to this
  view — signals are append-only, and a test asserts zero are bound.

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-25b: engagement view

Est. size: engagement.html ~70, engagement.js ~60, tests ~90. Total ~220.
