# PORTION P-26b — app: main() entry point

Goal: app/main.py real per C3.0 — the one way to start the tool:
run() builds the REAL ServiceKit from config, binds uvicorn to loopback on
the configured port, and opens the window through the P-26a adapter.

Contract: C3.0 (composition root) · 00-ARCHITECTURE (run + UI rows)

Files allowed (R10 budget: 2 source files + their tests):
  SOURCE   app/main.py
  TESTS    tests/app/test_main.py
  REGISTER README.md (LINE ADD ONLY: the "Daily use" section with the run
           command — the full README rewrite is P-29)

Context Pack: this card · C3.0 · app/config.py and app/window.py SIGNATURES from
  P-26a · the create_app SIGNATURE from P-20.

Done when:
  - run(workdir=None) against an existing config.json: the kit is REAL
    (assert kit.data is data.DataKit by class identity, not duck-typing),
    the server binds the configured port, and the window target URL is
    127.0.0.1:port. One test, three assertions.
  - run() binds loopback ONLY; a non-loopback configuration raises before
    the server starts.
  - the module docstring contains the exact run command that the README
    "Daily use" section contains (string equality assertion — README and
    code are the same truth, and P-29 re-asserts this).

Forbidden: building a stub kit in any branch of run(); a second entry
  point; reading the window library directly (go through the adapter).

Stop and ask if: a file named in Files allowed does not exist and the card does not say to create it; the contract section named above cannot be found after ONE search; a Done-when bullet needs a file outside Files allowed. Do not survey the repository — read the card, the named contract sections, and the allowed files only.

Commit: P-26b: main entry point

Est. size: main ~140, tests ~120. Total ~260.
